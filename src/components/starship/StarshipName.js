import React from 'react';

const PhotoCardAuthorTitle = ({name}) => {
  return (
    <div className="starship-name">
      Name: <span>{name}</span>
    </div>
  );
};

export default PhotoCardAuthorTitle;
